deploy-dev:
  command: gcloud app deploy --no-promote --version=dev

deploy-live:
  command: gcloud app deploy --no-promote --version=live

grow-build:
  command: grow build

serve:
  command: grow run --host=0.0.0.0 --port=4000
